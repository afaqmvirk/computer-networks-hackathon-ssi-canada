# Dashboard images

Place image assets here. Replace placeholders with real assets using the notes below.

| File | Purpose | Suggested size | Find on web |
|------|---------|----------------|--------------|
| `logo.svg` | Header logo next to "LoRaWAN Dataset" | 32–40px height | "LoRa Alliance logo" / https://lora-alliance.org/ (media/brand) |
| `device-placeholder.svg` | Device card: icon for selected device type | 48×48px | "IoT device icon" / "sensor icon SVG" / Heroicons |
| `site-banner-placeholder.svg` | Fallback when no banner image exists | 400×120px or wider | — |
| **Site banners (place in this folder)** | **Used when gateway is selected in Site view** | | |
| `carleton.png` | **Synthetic gateway** (`synthetic-gateway-01`) — Carleton image | 400×120px or wider | — |
| `kanata.png` | **Kanata** gateway (ID ending in `e250`, e.g. `00800000a000e250`) | 400×120px or wider | — |
| `site-great-slave-lake-1.png` | **Great Slave Lake** — gateway `008000000002aa4b` | 400×120px or wider | — |
| `site-great-slave-lake-2.png` | **Great Slave Lake** — fallback for all other gateways | 400×120px or wider | — |
| `site-banner-<gateway_id>.jpg` or `.png` | Any other gateway (sanitized ID in filename) | 400×120px or wider | Per-gateway map or photo |
| `site-banner.jpg` or `site-banner.png` | Shared fallback when no per-gateway image exists | 400×120px or wider | — |
| `nav-*.svg` or `nav-placeholder.svg` | Icons before nav links (Level, Soil, etc.) | 18–24px | "IoT icons" / Heroicons / Feather Icons |
| `empty-state.svg` | Empty states (None detected, No events) | 64–80px | "empty state illustration" / undraw.co |
| `about-lora.svg` | About section diagram (device–gateway–network) | ~200px wide | "LoRaWAN architecture diagram" |
| `icon-warning.svg` | Error / API-unavailable icon | 24×24px | "warning icon SVG" (or use CSS-only) |
